import { References } from "../references.js"
import { Base } from "./base.js"

export abstract class Leaf<DefType> extends Base<DefType> {
    // references(args: References.Options) {
    //     return [this.defToString()]
    // }
}

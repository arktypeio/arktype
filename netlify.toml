[build]
  command = "NODE_ENV=production && npm install -g @microsoft/rush@5.11.1 && rush update && rush build --to @re-do/website"
  publish = "pkgs/website/dist"
[build.environment]
  NODE_VERSION = "10.16.0"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

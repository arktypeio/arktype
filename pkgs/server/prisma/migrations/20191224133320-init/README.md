# Migration `20191224133320-init`

This migration has been generated by David Blass at 12/24/2019, 1:33:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Tag" (
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "test" INTEGER    REFERENCES "Test"(id) ON DELETE SET NULL,
  "user" INTEGER NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT
);

CREATE TABLE "lift"."Selector" (
  "css" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL
);

CREATE TABLE "lift"."Step" (
  "action" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "selector" INTEGER NOT NULL   REFERENCES "Selector"(id) ON DELETE RESTRICT,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL,
  "value" TEXT NOT NULL DEFAULT ''
);

CREATE TABLE "lift"."Test" (
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "user" INTEGER NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT
);

CREATE TABLE "lift"."User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "first" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "last" TEXT NOT NULL DEFAULT ''  ,
  "password" TEXT NOT NULL DEFAULT ''
);

CREATE TABLE "lift"."_StepToTest" (
  "A" INTEGER NOT NULL   REFERENCES "Step"(id) ON DELETE CASCADE,
  "B" INTEGER NOT NULL   REFERENCES "Test"(id) ON DELETE CASCADE
);

CREATE UNIQUE INDEX "lift"."Tag.name_user" ON "Tag"("name","user")

CREATE UNIQUE INDEX "lift"."Test.name_user" ON "Test"("name","user")

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "lift"."_StepToTest_AB_unique" ON "_StepToTest"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191224133320-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,50 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Tag {
+  id   Int    @id
+  user User
+  name String
+  @@unique([name, user])
+}
+
+model Selector {
+  id  Int    @id
+  css String
+}
+
+model Step {
+  id       Int      @id
+  action   String
+  selector Selector
+  value    String
+  tests    Test[]
+}
+
+model Test {
+  id    Int    @id
+  user  User
+  name  String
+  steps Step[]
+  tags  Tag[]
+  @@unique([name, user])
+}
+
+model User {
+  id        Int        @id
+  email     String     @unique
+  password  String
+  first     String
+  last      String
+  steps     Step[]
+  selectors Selector[]
+  tags      Tag[]
+  tests     Test[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191224133320-init)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20191224133320-init"

const photon = new Photon()

async function main() {
    const result = await photon.users()
    console.dir(result, { depth: null })
}

main()
```

import { assert } from "@re-/assert"
import { define } from "./internal.js"

export const testList = () => {
    describe("type", () => {
        test("is okay", () => {})
        describe("errors", () => {})
    })
}

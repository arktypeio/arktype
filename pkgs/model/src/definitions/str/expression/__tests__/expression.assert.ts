import { assert } from "@re-/assert"
import { define } from "@re-/model"

export const testList = () => {
    describe("type", () => {
        test("union", () => {})
        describe("errors", () => {})
    })
}

[build.environment]
  NODE_ENV = "production"
  NETLIFY_USE_YARN = "false"
[build]
  command = "npm install -g @microsoft/rush@5.50.0 && rush update && rush build && npm run build"
  publish = "build"


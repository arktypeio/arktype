# Migration `20190724093022-init`

This migration has been generated by David Blass at 7/24/2019, 9:30:23 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Test_tags"("nodeId" TEXT NOT NULL  REFERENCES Test(id),"position" INTEGER NOT NULL  ,"value" TEXT NOT NULL  ,PRIMARY KEY ("nodeId","position"));

CREATE TABLE "lift"."User_roles"("nodeId" TEXT NOT NULL  REFERENCES User(id),"position" INTEGER NOT NULL  ,"value" TEXT NOT NULL  ,PRIMARY KEY ("nodeId","position"));

ALTER TABLE "lift"."BrowserEvent" ADD COLUMN "test" TEXT   REFERENCES Test(id);

PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Test"("id" TEXT NOT NULL  ,"name" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

INSERT INTO "new_Test" ("id","name") SELECT "id","name" from "Test"

DROP TABLE "lift"."Test";

ALTER TABLE "lift"."new_Test" RENAME TO "Test";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User"("id" TEXT NOT NULL  ,"email" TEXT NOT NULL DEFAULT '' ,"password" TEXT NOT NULL DEFAULT '' ,"firstName" TEXT NOT NULL DEFAULT '' ,"lastName" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

INSERT INTO "new_User" ("id","email","password","firstName","lastName") SELECT "id","email","password","firstName","lastName" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190724084409-init..20190724093022-init
--- datamodel.dml
+++ datamodel.dml
@@ -17,17 +17,17 @@
 model Test {
   id String @default(cuid()) @id @unique
   name String
-  tags String
-  steps BrowserEvent
+  tags String[]
+  steps BrowserEvent[]
 }
 model User {
   id String @default(cuid()) @id @unique
   email String
   password String
-  roles String
+  roles String[]
   firstName String
   lastName String
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190724093022-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190724093022-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```

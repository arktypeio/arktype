# Migration `20190724084409-init`

This migration has been generated by David Blass at 7/24/2019, 8:44:09 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."BrowserEvent"("id" TEXT NOT NULL  ,"type" TEXT NOT NULL DEFAULT '' ,"selector" TEXT NOT NULL DEFAULT '' ,"value" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Test"("id" TEXT NOT NULL  ,"name" TEXT NOT NULL DEFAULT '' ,"tags" TEXT NOT NULL DEFAULT '' ,"steps" TEXT NOT NULL  REFERENCES BrowserEvent(id),PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."User"("id" TEXT NOT NULL  ,"email" TEXT NOT NULL DEFAULT '' ,"password" TEXT NOT NULL DEFAULT '' ,"roles" TEXT NOT NULL DEFAULT '' ,"firstName" TEXT NOT NULL DEFAULT '' ,"lastName" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "file:dev"."BrowserEvent.id._UNIQUE" ON "BrowserEvent"("id")

CREATE UNIQUE INDEX "file:dev"."Test.id._UNIQUE" ON "Test"("id")

CREATE UNIQUE INDEX "file:dev"."User.id._UNIQUE" ON "User"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190724084409-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,33 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model BrowserEvent {
+  id String @default(cuid()) @id @unique
+  type String
+  selector String
+  value String
+}
+
+model Test {
+  id String @default(cuid()) @id @unique
+  name String
+  tags String
+  steps BrowserEvent
+}
+
+model User {
+  id String @default(cuid()) @id @unique
+  email String
+  password String
+  roles String
+  firstName String
+  lastName String
+}
+
```

## Photon Usage

You can use a specific Photon built for this migration (20190724084409-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190724084409-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```

# Migration `20190724104756-init`

This migration has been generated by David Blass at 7/24/2019, 10:47:56 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Session"("id" TEXT NOT NULL  ,"token" TEXT NOT NULL DEFAULT '' ,"user" TEXT NOT NULL  REFERENCES User(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "lift"."Session.id._UNIQUE" ON "Session"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190724093358-init..20190724104756-init
--- datamodel.dml
+++ datamodel.dml
@@ -30,4 +30,10 @@
   firstName String
   lastName String
 }
+model Session {
+  id String @default(cuid()) @id @unique
+  token String
+  user User
+}
+
```

## Photon Usage

You can use a specific Photon built for this migration (20190724104756-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190724104756-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```

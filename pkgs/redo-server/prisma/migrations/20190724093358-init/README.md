# Migration `20190724093358-init`

This migration has been generated by David Blass at 7/24/2019, 9:33:58 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX "lift"."User.email._UNIQUE" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190724093022-init..20190724093358-init
--- datamodel.dml
+++ datamodel.dml
@@ -23,9 +23,9 @@
 }
 model User {
   id String @default(cuid()) @id @unique
-  email String
+  email String @unique
   password String
   roles String[]
   firstName String
   lastName String
```

## Photon Usage

You can use a specific Photon built for this migration (20190724093358-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190724093358-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```

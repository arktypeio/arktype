name: Draft release from branch
on: workflow_dispatch
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - run: |
                  echo "Hello ${{ github.event.inputs.name }}!"
                  echo "- in ${{ github.event.inputs.home }}!"
